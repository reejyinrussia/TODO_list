<%= link_to 'New Task', new_page_path %>
<hr />
<h1>Tasks</h1>

<% @pages.each do |page| %>
  <div>

    <h3>
      <%= link_to page.title, page_path(page) %>
    </h3>

  
    <%= link_to 'Delete', page_path(page), method: :delete %>
  </div>
  <hr />
<% end %>
